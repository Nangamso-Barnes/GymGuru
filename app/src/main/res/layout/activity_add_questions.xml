<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/back1"
    tools:context=".Activity.AddQuestionActivity">

    <RelativeLayout
        android:id="@+id/titleBarLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/back_big"
            android:contentDescription="@string/back_button"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:padding="8dp"
            app:tint="@color/grey" />

        <TextView
            android:id="@+id/textView5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/add_question_title"
            android:textAlignment="center"
            android:textColor="@color/grey"
            android:textSize="26sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <EditText
        android:id="@+id/enterQuestion"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="24dp"
        android:background="@color/white"
        android:hint="@string/hint_enter_question"
        android:inputType="text"
        android:padding="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titleBarLayout" />
    <Spinner
    android:id="@+id/questionTagSpinner"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:background="@drawable/spinner_background_white"
    android:padding="16dp"
    app:layout_constraintEnd_toEndOf="@+id/enterQuestion"
    app:layout_constraintStart_toStartOf="@+id/enterQuestion"
    app:layout_constraintTop_toBottomOf="@+id/enterQuestion"/>

    <Button
        android:id="@+id/btnAdd"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:onClick="onClickedAddOneQ"
        android:text="@string/button_add"
        app:layout_constraintEnd_toEndOf="@+id/questionTagSpinner"
        app:layout_constraintStart_toStartOf="@+id/questionTagSpinner"
        app:layout_constraintTop_toBottomOf="@+id/questionTagSpinner" />

</androidx.constraintlayout.widget.ConstraintLayout>